
INSERT INTO a VALUES (0), (1), (2), (3);CREATE TABLE b (a_id int);
DROP TABLE a,b;
SET client_min_messages = error;
DROP TABLE a,b;
SET client_min_messages = error;
INSERT INTO b SELECT generate_series(1,1000) % 4;
SET client_min_messages = error;CREATE TABLE b (a_id int);
INSERT INTO a VALUES (0), (1), (2), (3);
INSERT INTO b SELECT generate_series(1,1000) % 4;
DROP VIEW IF EXISTS v2;
INSERT INTO b SELECT generate_series(1,1000) % 4;
DROP TABLE a,b;
SET client_min_messages = error;
DROP TABLE a,b;
SET client_min_messages = error;
DROP VIEW IF EXISTS v2;
SET client_min_messages = error;
INSERT INTO b SELECT generate_series(1,1000) % 4;
INSERT INTO a VALUES (0), (1), (2), (3);
DROP VIEW IF EXISTS v2;
DROP VIEW IF EXISTS v2;
DROP VIEW IF EXISTS v2;
DROP VIEW IF EXISTS v2;
SET client_min_messages = error;
SET client_min_messages = error;
SET allow_system_table_mods = ON;
SET client_min_messages = error;
INSERT INTO b SELECT generate_series(1,1000) % 4;
INSERT INTO b SELECT generate_series(1,1000) % 4;
DROP VIEW IF EXISTS v2;
SET client_min_messages = error;
SET client_min_messages = error;
SET client_min_messages = error;
DROP TABLE a,b;